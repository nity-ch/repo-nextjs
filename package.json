{
  "name": "@playground/nextjs-root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter @playground/next-web dev",
    "build": "pnpm --filter @playground/next-web build",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "//overrides": "Standalone-mode fallback for cross-repo deps. Declared once here so apps inside this repo can use plain semver. Ignored when the umbrella's package.json is the active workspace root.",
    "overrides": {
      "@playground/ui": "https://github.com/your-org/repo-shared/releases/download/ui-v0.1.0/playground-ui-0.1.0.tgz"
    }
  }
}
